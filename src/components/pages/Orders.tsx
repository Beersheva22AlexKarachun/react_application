import { Typography } from "@mui/material"

const Orders: React.FC = () =>
  <Typography sx={{ textAlign: "center", fontSize: { xs: "3em", sm: "2em", ls: "4em" } }} >Order Component</Typography>

export default Orders 